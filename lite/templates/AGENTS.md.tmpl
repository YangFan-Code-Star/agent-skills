# TODO(init): 项目名 — Agent 工作手册

> **这个仓库还没初始化。**
> 第一件事：运行 `/init`，回答 3 个问题，本文件与 docs/ 里的 `TODO(init)` 会被填成真实内容。
> 初始化完成后删掉这段引用块。

TODO(init): 一句话说明这个项目是什么、给谁用。

**这个文件每次对话都会被完整加载，所以只放"每次都需要"的内容。** 细节放 docs/，按需读取。

## 快速上手

```bash
TODO(init): 从零到能跑起来的命令序列
```

## 验证命令

| 目的 | 命令 |
| --- | --- |
| TODO(init): 测试 / 构建 / 类型检查等，没有就写"无" | TODO(init) |

## 目录地图

```
AGENTS.md          本文件。每次对话全量加载，目标 80 行内
.agents/skills/    技能：init、maintain。按 description 触发，正文按需加载
docs/plan.md       目标、口径、当前任务、明确不做
docs/log.md        决策 / 坑 / 纠正的队列，合并后清空
TODO(init): 源码、测试、数据目录，按项目实际补齐
```

## 铁律

1. **不确定就问，不要编。**
2. **改完自己跑与改动相关的验证命令。**
3. **破坏性操作先确认。**
4. **密钥和真实数据永不进仓库。**

## 术语

TODO(init): 项目里最容易混淆的 1~3 个词；没有就删掉本表。

## 文档地图

| 什么时候读 | 文件 |
| --- | --- |
| 项目为什么做、成功标准、当前任务、明确不做 | [docs/plan.md](docs/plan.md) |
| 做过的决定、踩过的坑、待合并的纠正 | [docs/log.md](docs/log.md) |

## 可用技能

| 什么时候用 | 技能 |
| --- | --- |
| 初始化或补充项目上下文 | `/init` |
| 任务收尾 / 文档该更新了 / 收件箱该合并了 | `/maintain` |

## 当前状态

TODO(init): 现在做到哪了、下一步是什么。

## 维护这份手册

- 改了命令、目录、铁律、术语 → 同一个提交里更新本文件。
- 任务结束把新决定 / 坑 / 纠正追加进 docs/log.md。
- 细节超过 5 行 → 移到 docs/。
- 目标 80 行以内，超了就该往 docs/ 搬。
