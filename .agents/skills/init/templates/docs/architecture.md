# 架构

## 形态

TODO(init): 一段话说清这是什么形态的系统（Web 应用 / CLI / 库 / 批处理 / 服务），以及最重要的那个前提。

前提很重要，因为它决定了哪些"简化"是合理的。例如"单用户本地应用"这个前提，意味着没有鉴权、没有多租户隔离是对的，而不是欠债。**把前提写出来，并在文末列出"如果前提变了要重新评估什么"。**

## 分层与数据流

TODO(init): 画出从入口到出口的数据流。

```
TODO(init): 用简单的文本图表示各层，标注每层的职责
入口
  ↓
业务逻辑
  ↓
存储 / 输出
```

**单向依赖，不允许反向：** TODO(init): 写清楚哪一层不能 import 哪一层。

这是架构文档里最有价值的一句话。"A 不能依赖 B"是可执行的判断标准，agent 能据此在写代码时自我否决；而"保持低耦合"不是。

## 关键组件

TODO(init): 每个核心模块一到两行，说它负责什么、不负责什么。

## 数据模型要点

TODO(init): 核心数据结构，以及每个字段"为什么是这样"。

重点写那些**看代码猜不出来的语义**：某个字段为什么允许为空、两个看起来重复的字段为什么都要、某个约束背后的业务含义。

## 关键取舍

| 取舍 | 结论 | 原因 |
| --- | --- | --- |
| TODO(init) | | [ADR NNNN](decisions/) |

## 如果前提变了

TODO(init): 列出"如果 ⟨某个前提⟩ 不再成立，需要重新设计的地方"。

写清楚就够了，**不要提前预留**。为一个可能永远不来的需求做的抽象，代价是当下每一天的复杂度。
