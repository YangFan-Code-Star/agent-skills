# 项目目标

**"用户到底想要什么"的唯一来源。** 其他文档讲系统怎么造，这份讲为什么造、为谁造、做到什么算成。

agent 在做取舍时（要不要加这个功能、这个边界情况值不值得处理、性能和简洁选哪个）应该读这里。

## 一句话

TODO(init): 这个项目做什么，给谁用。

## 为什么做

TODO(init): 在此之前你是怎么解决这个问题的？为什么那样不够用？

真实的动机比抽象的目标有用得多。"想学新技术"、"受不了每个月手工对一遍账"、"给团队省掉重复劳动"都是合法答案，写下来能让 agent 在取舍时站对位置。

## 成功标准

TODO(init): 一个可验证的标准，不要写"做得好用"。

好的标准长这样：
- "能把 A 格式转成 B，并且我每周真的在用它"
- "新人照 README 能在 10 分钟内跑起来"
- "月度报表从手工 2 小时降到 5 分钟"

## 使用者与场景

TODO(init): 谁在用（只有你自己 / 小团队 / 对外发布），在什么场景下用。

这一条决定了很多事：只有自己用，就不必写面向他人的文档、不必考虑向后兼容、可以为了简单牺牲通用性；对外发布则相反。**不要在只有自己用的阶段"顺手预留"多用户能力。**

## 约束

TODO(init): 时间、预算、技术能力、合规、外部依赖上的限制。

包括不那么技术的约束：你每周能投入多少时间、有没有必须在某个日期前可用、有没有你不熟悉因而不想引入的技术。

## 优先级取舍

当两件事冲突时，倾向哪边：

| 冲突 | 这个项目的倾向 |
| --- | --- |
| 快速可用 vs 长期可维护 | TODO(init) |
| 功能覆盖 vs 简单可靠 | TODO(init) |
| 自动化程度 vs 可控可审查 | TODO(init) |

## 明确不追求的

TODO(init): 哪些常见的"好东西"这个项目明确不要。

比如：不追求高并发、不追求跨平台、不追求零依赖、不做移动端。写下来能挡掉大量 agent 自作主张的"改进"。范围层面的排除项写在 [roadmap.md](roadmap.md)。
